{
  "name": "pi-stream-server",
  "version": "1.0.0",
  "private": true,
  "description": "MJPEG webcam stream server for Raspberry Pi (ffmpeg + express)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
